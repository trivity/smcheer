# Local-only / tooling
.claude/
.playwright-mcp/
.vercel/
node_modules/
*.log

# Images: keep only the two optimized web logos.
# Ignore everything else (original full-size sources, local screenshots, etc.)
*.png
!customprint-logo.png
!jr-rattlers-logo.png
